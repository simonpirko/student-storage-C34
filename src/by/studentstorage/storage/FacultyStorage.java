package by.studentstorage.storage;

import by.studentstorage.domain.Faculty;
import by.studentstorage.domain.Student;
import by.studentstorage.domain.User;

import java.util.List;

public interface FacultyStorage {
    //boolean save(Student student, String facultyName);
    //Faculty remove(Student student);
    //String updateFaculty(String facultyName, Student student);
    //List<Faculty> getAll();
    //List<Faculty> getAllByFacultyName(String name);
    //boolean contains(Faculty faculty);
}
